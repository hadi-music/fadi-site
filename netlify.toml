# Netlify Configuration for Vanilla Static Site
[build]
  # No build command needed as we're using ES Modules/CDN
  publish = "."

## SPA Redirects (Enabled for future clean URL support)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
